# Examples

The normal tests use compact ENA-shaped fixtures under `tests/fixtures`. Runtime archive outputs are intentionally not committed because raw-read files can be large and are reproducible from a preserved snapshot and manifest.

Use the end-to-end command sequence in [the quickstart](../docs/quickstart.md) against a small public project, and inspect the dry run before allowing sequence transfer.
